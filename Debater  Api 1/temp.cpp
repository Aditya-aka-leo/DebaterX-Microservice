#include<bits/stdc++.h>
using namespace std;

int solve(int i ,int prev vector<int> nums , int m)
{
    if(i == nums.size())
    return 0 ;
    if(nums[i]==-1)
    {
        for(int i = 1;i<nums[i])
    }
}
